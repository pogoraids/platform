type Banlist {
    id: Int!
    dexId: Int!
    species: String
    allowed: Boolean
    settingsId: Int
}

input BanlistInput {
    id: Int!
    dexId: Int
    species: String
    allowed: Boolean
    settingsId: Int
}

type Query {
    getFullBanlist(id: Int!, settingsId: Int): [Banlist]
    getAllowedBanlist(id: Int!, settingsId: Int): [Banlist]
}

type Mutation {
    createBanlist(data: BanlistInput): [Banlist]
    bulkCreateBanList(data: [BanlistInput]): [Banlist]
}